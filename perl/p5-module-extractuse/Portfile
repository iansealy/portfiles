# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           perl5 1.0

perl5.branches      5.26
perl5.setup         Module-ExtractUse 0.342

platforms           darwin
maintainers         nomaintainer
license             {Artistic-1 GPL}

#supported_archs     noarch

description         Module::ExtractUse - Find out what modules are used

long_description    Module::ExtractUse is basically a Parse::RecDescent \
                    grammar to parse Perl code. It tries very hard to find \
                    all modules (whether pragmas, Core, or from CPAN) used \
                    by the parsed code.

checksums           rmd160 1a63de7fe0dd568716e047d93c20f72ea3e0537a \
                    sha256 5d295d4c86f716f3db268964b2f6a5f06531d07eadcd3ff8a82d07f6c98fe61b

if {${perl5.major} != ""} {
    depends_build-append \
                    port:p${perl5.major}-test-deep \
                    port:p${perl5.major}-test-nowarnings
    depends_lib-append \
                    port:p${perl5.major}-parse-recdescent \
                    port:p${perl5.major}-pod-strip
}

perl5.use_module_build
